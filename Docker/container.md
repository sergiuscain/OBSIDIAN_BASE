`docker container create --name redis redis` - создает контейнер по образу redis. `--name redis` - указывает название redis.
`docker container stats` - отображает параметры ресурсов контейнера (память, cpu и прочее)
