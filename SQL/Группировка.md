## 📊 Группировка 
### -- Группировка (GROUP BY)
```sql
SELECT department, COUNT(*) AS emp_count FROM employees GROUP BY department;
```
### -- HAVING (фильтрация групп)
Для фильтрации сгруппированных значений вместо оператора WHERE, используется оператор HAVING. Единственная разница состоит в том, что WHERE фильтрует записи, a HAVING — группы.  
Следующий запрос:
```sql
SELECT product_id, SUM(sale_amount) AS total_sale_amount
FROM sales
GROUP BY product_id
HAVING SUM(sale_amount) > 100;
```

Выведет продукты с общей суммой продаж более 100:

```sql
+------------+-------------------+
| product_id | total_sale_amount |
+------------+-------------------+
| 101        | 280.00            |
| 102        | 240.00            |
| 103        | 120.00            |
+------------+-------------------+
```

Фильтрацию данных можно осуществлять и до группировки, с оператором WHERE. А после использовать фильтрацию с оператором HAVING.
```sql
SELECT store_id, SUM(sale_amount) AS total_sale_amount FROM sales WHERE sale_date BETWEEN '2023-10-01' AND '2023-10-05' GROUP BY store_id;
```
