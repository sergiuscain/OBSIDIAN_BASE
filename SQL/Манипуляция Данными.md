## Манипуляция Данными
### -- Вставка данных
```sql
INSERT INTO users (username, email) VALUES ('user1', 'user1@mail.com');
```
### -- Обновление данных
```sql
UPDATE users SET email = 'new@mail.com' WHERE id = 1;
```
### -- Удаление данных
```sql
DELETE FROM users WHERE id = 5;
```