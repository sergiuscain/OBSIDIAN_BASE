## 📊Агрегатные Функции
 **AVG** — вычисляет среднее значение столбца.
CEIL(x) или CEILING(x) -возвращает наибольшее целое число, не большее x.  
ROUND(x, y) - округляет число x до y знаков после запятой.
FLOOR(x) - возвращает наименьшее целое число, не меньшее x.
MOD(x, y) - возвращает остаток от деления x на y.
POWER(x, y) или x ^ y - возводит число x в степень y.
SQRT(x) - возвращает квадратный корень из числа x.
MAX — находит максимальное значение в столбце
MIN — находит минимальное значение в столбце
SUM - сумма всех значений
GROUP_CONCAT() - объединяет значения столбца в одну строку ([[примеры функций#^groupconcat]])
### COUNT — считает количество строк или не-NULL-значений
```sql
SELECT 
    AVG(salary) AS avg_salary,
    MAX(salary) AS max_salary,
    SUM(sales) AS total_sales
FROM employees;
```
## Функции для работы с текстом

1. CONCAT() - объединяет две или более строк в одну.
2. CONCAT_WS() - объединяет две или более строк в одну с разделителем.
3. LOWER() - преобразует строку в нижний регистр.
4. UPPER() - преобразует строку в верхний регистр.
5. LENGTH() - возвращает длину строки.
6. LEFT() - возвращает n левых символов строки.
7. RIGHT() - возвращает n правых символов строки.
8. SUBSTRING() - возвращает подстроку определенной длины, начиная с определенной  позиции.
9. SUBSTRING_INDEX(str, delimiter, count) - извлекает подстроки из строки до определенного количества вхождений указанного разделителя.
10. TRIM() - удаляет пробелы в начале и в конце строки.
11. REPLACE() - заменяет все вхождения указанной подстроки в строке на другую подстроку.

## Функции для  работы с датой
#. CURDATE() - возвращает текущую дату.
2. CURTIME() - возвращает текущее время.
3. NOW() - возвращает текущую дату и время.
4. DATE_FORMAT() - форматирует дату в указанном формате.
5. DATEDIFF() - возвращает количество дней между двумя датами 
6. DATE_ADD() - добавляет интервал времени к дате.
7. DATE_SUB() - вычитает интервал времени из даты.
8. TIMESTAMPDIFF()  - вычисление разницы между двумя датами или временными метками в указанных единицах времени.
9. YEAR(date), MONTH(date), DAY(date), ##### HOUR(time), MINUTE(time),SECOND(time) - возвращают нужный формат (только год, только месяц, только день и.т.д.)
[[ФОРМАТЫ ДАТЫ]]
### -- DATE_ADD()
```sql
SELECT DATE_ADD('2022-03-05', INTERVAL 5 DAY);
```
### -- DATE_SUB()
```sql
SELECT DATE_SUB('2022-03-10', INTERVAL 5 DAY);
```
### -- TIMESTAMPDIFF()
```sql
SELECT TIMESTAMPDIFF(YEAR, '2020-01-01', '2023-01-01') AS years_diff;
```
```sql
+------------+ 
| years_diff | 
+------------+
| 3          | 
+------------+
```
### -- DATE_FORMAT(date, format): форматирует дату в указанном формате.
```sql
SELECT DATE_FORMAT('2022-03-05', '%d-%m-%Y');
```

```sql
+---------------------------------------+
| DATE_FORMAT('2022-03-05', '%d-%m-%Y') |
+---------------------------------------+
| 05-03-2022                            |
+---------------------------------------+
```